import React from 'react';

type RegistrationStep2PropsType = {
    setStepOfRegistration: (step: number) => void
}

export const RegistrationStep2:React.FC<RegistrationStep2PropsType> = ({setStepOfRegistration}) => {
    return (
        <div>

        </div>
    );
};

