import React from 'react';

type RegistrationStep3PropsType = {
    setStepOfRegistration: (step: number) => void
}

export const RegistrationStep3:React.FC<RegistrationStep3PropsType> = ({setStepOfRegistration}) => {
    return (
        <div>

        </div>
    );
};

